<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.internous.team2506.model.mapper.TblCartMapper">

	<select id="findByUserId" resultType="jp.co.internous.team2506.model.domain.dto.CartDto">
	SELECT 
	  c.id              AS id,
	  c.user_id         AS user_id,
	  c.product_id      AS product_id,
	  c.product_count   AS product_count,
	  c.created_at      AS created_at,
	  c.updated_at      AS update_at,
	  p.product_name    AS product_name,
	  p.image_full_path AS image_full_path,
	  p.price           AS price
	FROM 
	  tbl_cart AS c 
	JOIN
	  mst_product AS p
	ON
	  p.id = c.product_id
	WHERE 
	  c.user_id = #{userId}
	ORDER BY 
	  c.updated_at DESC, 
	  c.created_at DESC
	</select>
	
	<delete id="deleteById">
	DELETE
	FROM
	   tbl_cart
	WHERE id IN
	   <foreach item="id" collection="checkedIds" open="(" separator="," close=")">
        #{id}
       </foreach>   
	</delete>
	
</mapper>